service: student-enrollment-serverless

provider:
  name: aws
  runtime: python3.9
  region: us-east-1

plugins:
  - serverless-python-requirements
 
custom:
  pythonRequirements:
    dockerizePip: non-linux

functions:
  enroll_student:
    handler: lambdas.enroll_student.main:handler


